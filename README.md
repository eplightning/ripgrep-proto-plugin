# Ripgrep plugin

Ripgrep plugin for proto.

## Installation

Register the plugin by adding the following to your `.prototools` file:
```toml
[plugins]
ripgrep = "github://eplightning/ripgrep-proto-plugin"
```
